name: sliver_snap
description: Allows you to use powerful snapping slivers with expanded and collapsed bars.
version: 1.0.1
homepage: 'https://github.com/A-Fawzyy/sliver-snap'

environment:
  sdk: '>=2.19.4 <=3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.18.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  dart_code_metrics: ^5.7.3
  lint: ^2.1.2

flutter:
